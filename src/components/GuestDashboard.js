import React from "react";

const GuestDashboard = () => {
  return <div>You don't have authorisation to view student data, please speak to Mark Palmer to become a coach in order to get access.</div>;
};

export default GuestDashboard;
